{
  "name":"Messenger Bot",
  "version":"1.1.0",
  "description":"A simple Messenger Bot that uses regex expressions",
  "options": {
    "verifyToken":{
      "required": true
    },
    "accessToken":[]
  }
}
